<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context=".ui.Fragments.CartFragment">
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        >
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/cart_fragment__product_recycler_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            />
        <com.google.android.material.divider.MaterialDivider
            android:id="@+id/cart_fragment__mat_divider_precio"
            app:layout_constraintTop_toBottomOf="@id/cart_fragment__product_recycler_view"
            android:layout_width="match_parent"
            app:layout_constraintVertical_weight="90"
            android:layout_height="0dp"/>
        <TextView
            app:layout_constraintVertical_weight="10"
            app:layout_constraintTop_toBottomOf="@id/cart_fragment__mat_divider_precio"
            android:text="Subtotal: ta caro"
            android:id="@+id/cart_fragment__price_text"
            android:layout_width="match_parent"
            android:layout_height="0dp"/>
    </androidx.constraintlayout.widget.ConstraintLayout>

</FrameLayout>